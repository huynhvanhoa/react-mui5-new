import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
    secondary: {
      main: "#ffc400",
    },
    mycolor: {
      main: "#33ab9f",
    },
  },
  typography: {
    fontSize: 50,
  },
});
